hydra:
  job:
    chdir: true  # to save objects in a working directory
  run:
    dir: .  # Disable Hydra's Directory Change
  runtime:
    version_base: '1.1'


random:
  seed: 42

mts_data_folder: data/mts
mts_running_dataset: settings_six
